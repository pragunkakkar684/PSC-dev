interface BreadcrumbsProps {
  items?: Array<{ label: string; href?: string }>;
}

/** Breadcrumb chrome is rendered by AdminShell. */
export function Breadcrumbs(_props: BreadcrumbsProps) {
  return null;
}
